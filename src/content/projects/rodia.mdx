---
year: 2025
title: 'Audio Classification: Romanian Dialect Identification with RoDia'
description: 'A speech-based dialect classifier trained on the RoDia dataset for Romanian regional language detection.'
heroImage: '../../../public/images/romanhero.png'
heroImageAlign: 'center'
---

import GithubCard from '@/components/GithubCard.astro'

## Overview

This project is a speech-based language classification system developed using the **RoDia** dataset. The classifier detects Romanian dialects from audio samples, covering five distinct regional dialects.

Built with Python and TensorFlow, the system demonstrates a full pipeline for preprocessing, training, and evaluating audio models in low-resource language settings.

## 🧩 Dataset Overview

The **RoDia** dataset focuses on dialect detection in Romanian speech and includes:

- **Ard**: Ardeal  
- **Ban**: Banat  
- **Mol**: Moldova  
- **Mun**: Muntenia  
- **Olt**: Oltenia  

### Structure

| Directory              | Labels                    |
|------------------------|---------------------------|
| `real_data/data/train` | Ard, Ban, Mol, Mun, Olt   |
| `real_data/data/test1` | Ard, Ban, Mol, Mun, Olt   |

Each label folder contains `.wav` audio files for that region's dialect.

## 🛠️ Environment Setup

Use either `venv` or `conda` for your environment:

### Option 1: venv (Recommended)

> ⚠️ Use Python 3.10 or below for TensorFlow compatibility.

```bash
python3.10 -m venv rodia_env
source rodia_env/bin/activate  # Windows: rodia_env\Scripts\activate
